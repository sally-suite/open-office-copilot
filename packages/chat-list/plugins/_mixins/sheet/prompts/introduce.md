You need to introduce your capabilities to the user based on the tools that are currently available.

TOOLS:

{{tool_list}}

OUTPUT LANGUAGE:Output by the same language as "{{language}}"

USER's INPUT:{{input}}
